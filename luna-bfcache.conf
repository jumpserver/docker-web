# Browser document navigations advertise text/html. Subresources do not, so
# an empty value keeps their immutable/hash-based caching behavior unchanged.
map $http_accept $luna_cache_control {
    default      "";
    ~*text/html  "no-store";
}
